{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Current File",
            "type": "dotnet",
            "request": "launch",
            "projectPath": "${file}"
        }
    ]
}